package com.app.graham.partner.utils

public class Networkcall {
   /* fun networkPostCall(application: Application, url: String, mQueue: RequestQueue, jsonObject: JSONObject, customProgressDialog: CustomProgessDialog, networkCallbacks: NetworkCallbacks, typedata: String){
        // HttpsTrustManager.allowAllSSL()
     //   customProgressDialog.custom_progress()
        var customVolleyRequest= CustomVolleyRequest(application, Request.Method.POST, url,jsonObject, Response.Listener<JSONObject>(){
            networkCallbacks.successCallback(it,typedata)
            customProgressDialog.hide_progress()


        }, PSNResponseError(application,customProgressDialog))
        customVolleyRequest.retryPolicy = DefaultRetryPolicy(10000, DefaultRetryPolicy.DEFAULT_MAX_RETRIES, DefaultRetryPolicy.DEFAULT_BACKOFF_MULT)
        mQueue?.add(customVolleyRequest)
    }*/
}